import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#455a64",
    },
  },
  shape: {
    borderRadius: 3,
  },
});

export default theme;
